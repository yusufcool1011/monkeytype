name: Deploy Monkeytype to GitHub Pages

on:
  push:
    branches:
      - master # Adjust if your default branch is 'main'

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18 # Or the matching version in package.json
          cache: 'npm'

      - name: Install Dependencies
        run: npm ci --legacy-peer-deps

      - name: Build Project
        run: npm run build
        env:
          # If the project supports specifying a public URL at build time:
          PUBLIC_URL: /monkeytype

      - name: Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist # Check if the build output folder is 'dist', 'build', or 'out'
          branch: gh-pages # The branch the action will push static files to
